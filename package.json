{
  "name": "ctx-lab",
  "version": "0.1.0",
  "description": "Turkce masaustu AI calisma hafizasi uygulamasi.",
  "author": "Cagri Sahin",
  "private": true,
  "type": "module",
  "main": "electron/main.mjs",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "desktop:dev": "node scripts/desktop-dev.mjs",
    "desktop": "electron .",
    "desktop:build": "npm run build && electron .",
    "desktop:smoke": "npm run build && node scripts/desktop-smoke.mjs && node scripts/electron-smoke-launch.mjs",
    "desktop:flow": "npm run build && node scripts/electron-flow-smoke.mjs",
    "desktop:pack": "npm run build && electron-builder --dir",
    "desktop:dist": "npm run build && electron-builder --win portable",
    "verify:desktop": "npm run desktop:smoke && npm run desktop:flow",
    "start:windows": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/start-windows.ps1",
    "runner": "node scripts/ctxlab-runner.mjs",
    "runner:check": "node scripts/ctxlab-runner.mjs --dry-run",
    "icon:generate": "node scripts/generate-icon.mjs",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "smoke": "node scripts/smoke-dist.mjs",
    "verify": "npm test && npm run build && npm run smoke"
  },
  "devDependencies": {
    "electron": "^42.0.1",
    "electron-builder": "^26.8.1",
    "playwright-core": "^1.60.0",
    "vite": "^6.0.0"
  }
}
